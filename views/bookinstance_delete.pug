extends layout

block content

  h1 Delete Book Instance Id: #{bookinstance._id}
 
    p Do you really want to delete this BookInstance?

    form(method='POST')
      div.form-group
        input#authorid.form-control(type='hidden', name='bookinstanceid', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete
